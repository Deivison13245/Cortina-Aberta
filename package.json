{
  "name": "cortina-aberta",
  "version": "1.0.0",
  "scripts": {
    "build": "rimraf dist && mkdir dist && npm run build:css && npm run build:js && npm run build:html && npm run copy:assets",
    "build:css": "esbuild assets/css/style.css --minify --outdir=dist/assets/css",
    "build:js": "esbuild assets/js/main.js sw.js --minify --outdir=dist",
    "build:html": "html-minifier-terser --collapse-whitespace --remove-comments --minify-js true --minify-css true index.html -o dist/index.html",
    "copy:assets": "cpx \"assets/images/**/*\" dist/assets/images && cpx \"assets/downloads/**/*\" dist/assets/downloads && cpx manifest.json dist/"
  },
  "devDependencies": {
    "cpx2": "^7.0.0",
    "esbuild": "^0.20.0",
    "html-minifier-terser": "^7.2.0",
    "rimraf": "^5.0.0"
  }
}
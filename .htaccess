# Ativar o módulo de Cache / Expiração
<IfModule mod_expires.c>
    ExpiresActive On

    # Mídia / Imagens (Cache longo de 1 ano)
    ExpiresByType image/jpg                    "access plus 1 year"
    ExpiresByType image/jpeg                   "access plus 1 year"
    ExpiresByType image/gif                    "access plus 1 year"
    ExpiresByType image/png                    "access plus 1 year"
    ExpiresByType image/webp                   "access plus 1 year"
    ExpiresByType image/svg+xml                "access plus 1 year"
    ExpiresByType image/x-icon                 "access plus 1 year"

    # Vídeos e Áudio
    ExpiresByType video/mp4                    "access plus 1 year"
    ExpiresByType video/webm                   "access plus 1 year"

    # CSS e JavaScript (Cache de 1 mês)
    ExpiresByType text/css                     "access plus 1 month"
    ExpiresByType text/javascript              "access plus 1 month"
    ExpiresByType application/javascript       "access plus 1 month"

    # Fontes Web
    ExpiresByType font/ttf                     "access plus 1 year"
    ExpiresByType font/otf                     "access plus 1 year"
    ExpiresByType font/woff                    "access plus 1 year"
    ExpiresByType font/woff2                   "access plus 1 year"

    # Documentos HTML e Dados (Sem cache ou expiração curta)
    ExpiresByType text/html                    "access plus 0 seconds"
    ExpiresByType application/json             "access plus 0 seconds"
    
    # Tempo padrão de fallback
    ExpiresDefault                             "access plus 2 days"
</IfModule>

# Definir cabeçalhos Cache-Control explicitamente
<IfModule mod_headers.c>
    # 1 Ano para recursos estáticos (Imagens, Fontes)
    <FilesMatch "\.(ico|pdf|flv|jpg|jpeg|png|gif|webp|js|css|swf|woff|woff2|ttf|otf)$">
        Header set Cache-Control "max-age=31536000, public"
    </FilesMatch>

    # Não armazenar em cache arquivos dinâmicos ou HTML
    <FilesMatch "\.(html|htm|php)$">
        Header set Cache-Control "max-age=0, private, no-store, no-cache, must-revalidate"
    </FilesMatch>
</IfModule>